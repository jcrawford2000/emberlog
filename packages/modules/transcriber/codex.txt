Create docs/CURRENT_STATE.md. First, inspect the repository structure (top-level and key subfolders). 
Document what exists in the repo today:
- What components exist (backend, api, web, notifier, etc.) and where
- Config
- Add a section 'Local Development Constraints' that explains why the system can't run end-to-end on a typical workstation (missing pipeline + GPU Whisper dependency).  
- Key entry points (main modules, CLI entry, FastAPI app object, systemd units, k8s manifests)
- Data flow as implemented (not aspirational)
Keep it factual; if unsure, label as 'Unknown/Needs verification'.


Update docs/CURRENT_STATE.md:
- Add a section 'System Context (External Repos)' describing the 3-repo architecture:
  - emberlog: watcher/worker/transcriber and sinks
  - emberlog-api: REST + Postgres + SSE
  - emberlog-web: frontend consuming REST/SSE
- Make clear the API server is intentionally not in this repo.


Create AGENTS.md at repo root. Include:
- What Emberlog is and is not (non-goals)
- Repo scope: emberlog backend worker only
- Explicitly note emberlog-api and emberlog-web exist and are separate repos
- Rules: do not add API server or frontend code to this repo; prefer mocks/stubs for local dev
- How to run/lint/test (as known)
- Coding conventions: type hints, docstrings, structured logging, minimal coupling
Keep concise.


Create REQUIREMENTS.md describing Emberlog requirements based on CURRENT_STATE plus existing behavior. Include:
- Functional requirements (ingest, transcribe, parse, store, notify, UI)
- Non-functional requirements (latency, reliability, observability, security)
- Out of scope
Do not invent features; mark aspirational items separately.
Keep concise.


Create docs/DESIGN.md based on docs/CURRENT_STATE.md abd docs/REQUIREMENTS.md. Include:
- High-level architecture diagram in ASCII (simple)
- Data flow / sequence from file ingest to final persistence/notification
- Module/service responsibilities
- Database schema summary (if any)
- Error handling strategy
If there is a mismatch between current implementation and intended design, create a section 'Gaps / Refactors'.


Create docs/feature-tracker.md. Seed it by scanning open TODOs, issues, unfinished modules, gaps, and obvious missing pieces from docs/CURRENT_STATE, docs/REQUIREMENTS, and docs/DESIGN. 
Use tags [Planned], [In Progress], [Blocked], [On Hold], [Done]. For each item include:
- Summary
- Acceptance criteria
- Components touched
- Notes / risks
Keep it pragmatic and short.

Create docs/INTEGRATIONS.md describing how this repo integrates with emberlog-api:
- Required REST endpoints used by ApiSink (method/path/payload fields)
- Expected responses and error handling expectations
- SSE is owned by emberlog-api (note only; no implementation here)
If exact endpoint details aren't discoverable from this repo, add placeholders marked 'Needs verification' and list what must be filled in.

