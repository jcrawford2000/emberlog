apiVersion: v1
kind: Service
metadata:
  name: emberlog-web
  namespace: emberlog
spec:
  type: ClusterIP
  selector:
    app: emberlog-web
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      