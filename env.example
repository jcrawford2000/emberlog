# Emberlog (Monorepo) - Root environment example
# Copy to .env ONLY if you want a shared file for local convenience.
# Individual packages also have their own .env.example files.

# Optional: identify this environment (useful for logs)
EMBERLOG_ENV=dev
